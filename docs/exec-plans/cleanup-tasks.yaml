generated_at: "2026-03-24T00:00:00Z"
generated_from_head: "2cccee2"
task_count: 8
tasks:
  - id: cleanup-mb011-harness-skills-plugins-loader-py
    principle_id: "MB011"
    principle_category: "architecture"
    severity: "blocking"
    title: "Enforce architecture rule: LOGGING PROVIDER — all production code must obtain (MB011)"
    scope:
      - harness_skills/plugins/loader.py
      - harness_skills/plugins/runner.py
      - harness_tools/stale_plan_detector.py
    description: |
      Principle MB011 (architecture/blocking) is violated in 3 file(s).

      Rule: LOGGING PROVIDER — all production code must obtain loggers exclusively via
      `from harness_skills.logging_config import get_logger` followed by
      `logger = get_logger("<domain>")`. Calling `logging.getLogger()` directly
      in domain code is forbidden; it bypasses trace-ID injection, domain-field
      tagging, and the structured JSON formatter configured by `configure()`.
      The only permitted direct use of `logging.getLogger()` is inside
      harness_skills/logging_config.py itself (the provider implementation).

      Affected files:
        - harness_skills/plugins/loader.py:8  — logger = logging.getLogger("harness_skills.plugins.loader")
        - harness_skills/plugins/runner.py:7  — logger = logging.getLogger("harness_skills.plugins.runner")
        - harness_tools/stale_plan_detector.py:44  — log = logging.getLogger("stale_detector")

      Refactoring steps:
        1. In each file, replace `import logging` + `logging.getLogger(...)` with
           `from harness_skills.logging_config import get_logger` and
           `logger = get_logger("<domain>")` at module level using the existing domain
           name (e.g. "harness_skills.plugins.loader").
        2. Run `/harness:lint --gate principles` to verify zero remaining violations.
        3. Update or add unit/integration tests covering the refactored code.
    pr_title: "refactor: enforce MB011 architecture across harness_skills/plugins/loader.py, harness_skills/plugins/runner.py, harness_tools/stale_plan_detector.py"
    pr_body: |
      ## What & Why

      Enforces principle MB011 (architecture/blocking):
      "LOGGING PROVIDER — all production code must obtain loggers exclusively via
      `from harness_skills.logging_config import get_logger` followed by
      `logger = get_logger("<domain>")`. Calling `logging.getLogger()` directly
      in domain code is forbidden."

      This PR was generated automatically by `/golden-principles-cleanup` after
      detecting 3 violation(s) across the following files:
      - harness_skills/plugins/loader.py
      - harness_skills/plugins/runner.py
      - harness_tools/stale_plan_detector.py

      ## Changes

      - harness_skills/plugins/loader.py: replace `logging.getLogger(...)` with `get_logger("harness_skills.plugins.loader")`
      - harness_skills/plugins/runner.py: replace `logging.getLogger(...)` with `get_logger("harness_skills.plugins.runner")`
      - harness_tools/stale_plan_detector.py: replace `logging.getLogger("stale_detector")` with `get_logger("stale_detector")`
      - Updated affected call-sites to use the new abstraction

      ## Testing

      - [ ] All refactored code has unit tests
      - [ ] No violations remain: `uv run python -m harness_skills.cli.main evaluate --gate principles --format json`
      - [ ] `/harness:lint` passes with 0 blocking violations
      - [ ] Existing test suite passes: `pytest tests/ -v`

      ## Checklist

      - [ ] PR title follows conventional commits (`refactor:` prefix)
      - [ ] No unrelated changes included
      - [ ] docs/PRINCIPLES.md is up to date (run `/define-principles` if needed)
    generated_at: "2026-03-24T00:00:00Z"
    status: "pending"

  - id: cleanup-mb013-harness-skills-stale-plan-detector-py
    principle_id: "MB013"
    principle_category: "architecture"
    severity: "blocking"
    title: "Enforce architecture rule: SECRETS / AUTH PROVIDER — direct os.environ.get() (MB013)"
    scope:
      - harness_skills/stale_plan_detector.py
      - harness_skills/plugins/gate_plugin.py
      - harness_tools/checkpoint_agent.py
    description: |
      Principle MB013 (architecture/blocking) is violated in 3 file(s).

      Rule: SECRETS / AUTH PROVIDER — direct `os.environ.get()` / `os.getenv()` calls
      for sensitive values (API keys, tokens, credentials, secrets) are permitted
      only in designated bootstrap or top-level entry-point modules.
      All domain packages and sub-packages must receive credentials as constructor
      arguments or function parameters — never call os.environ inline inside a
      domain service, gate, plugin, or model.

      Affected files:
        - harness_skills/stale_plan_detector.py:464  — os.environ.get("ANTHROPIC_API_KEY") inside domain class method
        - harness_skills/plugins/gate_plugin.py:61  — {**os.environ, **expanded_env} merges environment inside gate class
        - harness_tools/checkpoint_agent.py:59  — AGENT_ID = os.environ.get("AGENT_ID", ...) / TASK_ID = os.environ.get("TASK_ID", ...)

      Refactoring steps:
        1. In harness_skills/stale_plan_detector.py: remove os.environ.get("ANTHROPIC_API_KEY")
           from the domain class method; add an `api_key` parameter to the constructor
           and thread it through to the call site.
        2. In harness_skills/plugins/gate_plugin.py: move the `{**os.environ, **expanded_env}`
           env-merging to the CLI/entry-point layer; pass the merged dict as a parameter.
        3. In harness_tools/checkpoint_agent.py: move module-level env reads to the
           `if __name__ == "__main__"` block or a designated bootstrap function,
           then pass values as arguments to the domain logic.
        4. Run `/harness:lint --gate principles` to verify zero remaining violations.
        5. Update or add unit/integration tests covering the refactored code.
    pr_title: "refactor: enforce MB013 architecture across 3 files"
    pr_body: |
      ## What & Why

      Enforces principle MB013 (architecture/blocking):
      "SECRETS / AUTH PROVIDER — direct `os.environ.get()` / `os.getenv()` calls
      for sensitive values are permitted only in designated bootstrap or top-level
      entry-point modules."

      This PR was generated automatically by `/golden-principles-cleanup` after
      detecting 3 violation(s) across the following files:
      - harness_skills/stale_plan_detector.py
      - harness_skills/plugins/gate_plugin.py
      - harness_tools/checkpoint_agent.py

      ## Changes

      - harness_skills/stale_plan_detector.py: accept api_key as constructor parameter instead of os.environ.get inline
      - harness_skills/plugins/gate_plugin.py: move env dict preparation to entry-point layer; accept pre-merged dict
      - harness_tools/checkpoint_agent.py: move os.environ reads to bootstrap block; pass values as parameters
      - Updated affected call-sites to use the new abstraction

      ## Testing

      - [ ] All refactored code has unit tests
      - [ ] No violations remain: `uv run python -m harness_skills.cli.main evaluate --gate principles --format json`
      - [ ] `/harness:lint` passes with 0 blocking violations
      - [ ] Existing test suite passes: `pytest tests/ -v`

      ## Checklist

      - [ ] PR title follows conventional commits (`refactor:` prefix)
      - [ ] No unrelated changes included
      - [ ] docs/PRINCIPLES.md is up to date (run `/define-principles` if needed)
    generated_at: "2026-03-24T00:00:00Z"
    status: "pending"

  - id: cleanup-mb003-tests-gates-test-runner-plugin-integration-py
    principle_id: "MB003"
    principle_category: "architecture"
    severity: "blocking"
    title: "Enforce architecture rule: harness_skills/gates: __all__ is now declared (MB003)"
    scope:
      - tests/gates/test_runner_plugin_integration.py
      - tests/gates/test_coverage.py
      - tests/gates/test_docs_freshness.py
    description: |
      Principle MB003 (architecture/blocking) is violated in 3 file(s).

      Rule: harness_skills/gates: __all__ is now declared, listing:
        CoverageGate, DocsFreshnessGate, DocsGateConfig,
        EvaluationSummary, GateEvaluator, GateFailure, GateOutcome,
        HarnessConfigLoader, run_gates.
      All imports from outside harness_skills/gates/ must go through
      harness_skills.gates — never from harness_skills.gates.<module> directly.
      DEEP IMPORT VIOLATIONS and PRIVATE SYMBOL VIOLATIONS must be refactored.

      Affected files:
        - tests/gates/test_runner_plugin_integration.py:20  — imports GateEvaluator, run_gates, EvaluationSummary from harness_skills.gates.runner directly
        - tests/gates/test_coverage.py:34  — imports private symbols _detect_format, _parse_json, _parse_lcov, _parse_xml, _ParseError from harness_skills.gates.coverage
        - tests/gates/test_docs_freshness.py:8  — imports private symbols _extract_file_refs, _looks_like_file_path, _parse_generated_at from gates.docs_freshness

      Refactoring steps:
        1. In tests/gates/test_runner_plugin_integration.py:20, replace
           `from harness_skills.gates.runner import GateEvaluator, run_gates, EvaluationSummary`
           with `from harness_skills.gates import GateEvaluator, run_gates, EvaluationSummary`.
        2. In tests/gates/test_coverage.py and tests/gates/test_docs_freshness.py,
           refactor tests to stop importing private implementation details; test only
           the public gate interface (CoverageGate.run(), DocsFreshnessGate.run()).
        3. Run `/harness:lint --gate principles` to verify zero remaining violations.
        4. Update or add unit/integration tests covering the refactored code.
    pr_title: "refactor: enforce MB003 architecture across test_runner_plugin_integration.py, test_coverage.py, test_docs_freshness.py"
    pr_body: |
      ## What & Why

      Enforces principle MB003 (architecture/blocking):
      "All imports from outside harness_skills/gates/ must go through
      harness_skills.gates — never from harness_skills.gates.<module> directly."

      This PR was generated automatically by `/golden-principles-cleanup` after
      detecting 3 violation(s) across the following files:
      - tests/gates/test_runner_plugin_integration.py
      - tests/gates/test_coverage.py
      - tests/gates/test_docs_freshness.py

      ## Changes

      - tests/gates/test_runner_plugin_integration.py: update deep import to use harness_skills.gates public surface
      - tests/gates/test_coverage.py: refactor to test CoverageGate.run() instead of private _parse_* functions
      - tests/gates/test_docs_freshness.py: refactor to test DocsFreshnessGate.run() instead of private helpers
      - Updated affected call-sites to use the new abstraction

      ## Testing

      - [ ] All refactored code has unit tests
      - [ ] No violations remain: `uv run python -m harness_skills.cli.main evaluate --gate principles --format json`
      - [ ] `/harness:lint` passes with 0 blocking violations
      - [ ] Existing test suite passes: `pytest tests/ -v`

      ## Checklist

      - [ ] PR title follows conventional commits (`refactor:` prefix)
      - [ ] No unrelated changes included
      - [ ] docs/PRINCIPLES.md is up to date (run `/define-principles` if needed)
    generated_at: "2026-03-24T00:00:00Z"
    status: "pending"

  - id: cleanup-mb004-harness-skills-cli-evaluate-py
    principle_id: "MB004"
    principle_category: "architecture"
    severity: "blocking"
    title: "Enforce architecture rule: harness_skills/generators: __all__ is declared (MB004)"
    scope:
      - harness_skills/cli/evaluate.py
      - harness_skills/cli/create.py
      - harness_skills/cli/manifest.py
      - harness_skills/generators/config_generator.py
      - tests/test_generators/test_evaluation.py
      - tests/test_generators/test_manifest_generator.py
      - tests/test_cli/test_manifest_cmd.py
    description: |
      Principle MB004 (architecture/blocking) is violated in 7 file(s).

      Rule: harness_skills/generators: __all__ is declared and the boundary is EXPLICIT.
      All imports from outside harness_skills/generators/ must go through
      harness_skills.generators (the subpackage root) — never from
      harness_skills.generators.<module> directly.

      Affected files:
        - harness_skills/cli/evaluate.py:40  — deep import from generators.evaluation
        - harness_skills/cli/create.py:40  — deep import from generators.config_generator
        - harness_skills/cli/manifest.py:118  — deep import from generators.manifest_generator (inline import)
        - harness_skills/generators/config_generator.py:31  — deep import from models.gate_configs (cross-domain, see MB005)
        - tests/test_generators/test_evaluation.py:20  — deep import from generators.evaluation
        - tests/test_generators/test_manifest_generator.py:21  — deep import from generators.manifest_generator
        - tests/test_cli/test_manifest_cmd.py:23  — deep import from generators.manifest_generator

      Refactoring steps:
        1. Add generate_manifest, write_manifest_pair, generate_gate_config, write_harness_config
           to harness_skills/generators/__init__.py __all__ as prerequisites.
        2. Update all callers to import from harness_skills.generators instead of
           harness_skills.generators.<module>.
        3. Fix the cross-domain import in config_generator.py (see MB005 for models boundary).
        4. Run `/harness:lint --gate principles` to verify zero remaining violations.
        5. Update or add unit/integration tests covering the refactored code.
    pr_title: "refactor: enforce MB004 architecture across 7 files"
    pr_body: |
      ## What & Why

      Enforces principle MB004 (architecture/blocking):
      "All imports from outside harness_skills/generators/ must go through
      harness_skills.generators (the subpackage root) — never from
      harness_skills.generators.<module> directly."

      This PR was generated automatically by `/golden-principles-cleanup` after
      detecting 7 violation(s) across the following files:
      - harness_skills/cli/evaluate.py
      - harness_skills/cli/create.py
      - harness_skills/cli/manifest.py
      - harness_skills/generators/config_generator.py
      - tests/test_generators/test_evaluation.py
      - tests/test_generators/test_manifest_generator.py
      - tests/test_cli/test_manifest_cmd.py

      ## Changes

      - harness_skills/generators/__init__.py: export generate_manifest, write_manifest_pair, generate_gate_config, write_harness_config in __all__
      - harness_skills/cli/evaluate.py: update import to use harness_skills.generators public surface
      - harness_skills/cli/create.py: update import to use harness_skills.generators public surface
      - harness_skills/cli/manifest.py: update inline import to use harness_skills.generators
      - All test files: update deep imports to use harness_skills.generators
      - Updated affected call-sites to use the new abstraction

      ## Testing

      - [ ] All refactored code has unit tests
      - [ ] No violations remain: `uv run python -m harness_skills.cli.main evaluate --gate principles --format json`
      - [ ] `/harness:lint` passes with 0 blocking violations
      - [ ] Existing test suite passes: `pytest tests/ -v`

      ## Checklist

      - [ ] PR title follows conventional commits (`refactor:` prefix)
      - [ ] No unrelated changes included
      - [ ] docs/PRINCIPLES.md is up to date (run `/define-principles` if needed)
    generated_at: "2026-03-24T00:00:00Z"
    status: "pending"

  - id: cleanup-mb005-harness-skills-stale-plan-detector-py
    principle_id: "MB005"
    principle_category: "architecture"
    severity: "blocking"
    title: "Enforce architecture rule: harness_skills/models: __all__ is declared (MB005)"
    scope:
      - harness_skills/stale_plan_detector.py
      - harness_skills/cli/status.py
      - harness_skills/cli/observe.py
      - harness_skills/plugins/gate_plugin.py
      - harness_skills/plugins/runner.py
      - harness_skills/telemetry_reporter.py
      - harness_skills/gates/coverage.py
      - harness_skills/gates/runner.py
      - harness_skills/generators/config_generator.py
    description: |
      Principle MB005 (architecture/blocking) is violated in 9 file(s).

      Rule: harness_skills/models: __all__ is declared and the boundary is EXPLICIT.
      All imports from outside harness_skills/models/ must go through
      harness_skills.models — never from harness_skills.models.<module> directly.

      Affected files:
        - harness_skills/stale_plan_detector.py:54  — from harness_skills.models.base import Severity, Status
        - harness_skills/stale_plan_detector.py:55  — from harness_skills.models.stale import (...)
        - harness_skills/cli/status.py:57  — from harness_skills.models.base import Status
        - harness_skills/cli/status.py:58  — from harness_skills.models.status import (...)
        - harness_skills/cli/observe.py:63  — from harness_skills.models.observe import LogEntry, ObserveResponse
        - harness_skills/plugins/gate_plugin.py:9  — from harness_skills.models.base import GateResult, Status, Violation
        - harness_skills/plugins/runner.py:4  — from harness_skills.models.base import GateResult, Status
        - harness_skills/telemetry_reporter.py:37  — from harness_skills.models.base import HarnessResponse, Status
        - harness_skills/telemetry_reporter.py:38  — from harness_skills.models.telemetry import (...)
        - harness_skills/gates/coverage.py:57  — from harness_skills.models.gate_configs import CoverageGateConfig
        - harness_skills/gates/runner.py:68  — from harness_skills.models.gate_configs import (...)
        - harness_skills/gates/runner.py:83  — from harness_skills.models.base import GateResult as BaseGateResult, Status
        - harness_skills/generators/config_generator.py:31  — from harness_skills.models.gate_configs import (...)

      Refactoring steps:
        1. Update each listed file to import from `harness_skills.models` instead of
           `harness_skills.models.<module>` — e.g. `from harness_skills.models import Status, Violation`.
        2. Verify all needed symbols are exported from harness_skills/models/__init__.py __all__.
        3. Run `/harness:lint --gate principles` to verify zero remaining violations.
        4. Update or add unit/integration tests covering the refactored code.
    pr_title: "refactor: enforce MB005 architecture across 9 files"
    pr_body: |
      ## What & Why

      Enforces principle MB005 (architecture/blocking):
      "All imports from outside harness_skills/models/ must go through
      harness_skills.models — never from harness_skills.models.<module> directly."

      This PR was generated automatically by `/golden-principles-cleanup` after
      detecting 9 violation(s) across the following files:
      - harness_skills/stale_plan_detector.py
      - harness_skills/cli/status.py
      - harness_skills/cli/observe.py
      - harness_skills/plugins/gate_plugin.py
      - harness_skills/plugins/runner.py
      - harness_skills/telemetry_reporter.py
      - harness_skills/gates/coverage.py
      - harness_skills/gates/runner.py
      - harness_skills/generators/config_generator.py

      ## Changes

      - All listed files: update sub-module imports to use `from harness_skills.models import X`
      - harness_skills/models/__init__.py: verify all required symbols are in __all__
      - Updated affected call-sites to use the new abstraction

      ## Testing

      - [ ] All refactored code has unit tests
      - [ ] No violations remain: `uv run python -m harness_skills.cli.main evaluate --gate principles --format json`
      - [ ] `/harness:lint` passes with 0 blocking violations
      - [ ] Existing test suite passes: `pytest tests/ -v`

      ## Checklist

      - [ ] PR title follows conventional commits (`refactor:` prefix)
      - [ ] No unrelated changes included
      - [ ] docs/PRINCIPLES.md is up to date (run `/define-principles` if needed)
    generated_at: "2026-03-24T00:00:00Z"
    status: "pending"

  - id: cleanup-mb006-tests-plugins-test-runner-py
    principle_id: "MB006"
    principle_category: "architecture"
    severity: "blocking"
    title: "Enforce architecture rule: harness_skills/plugins: __all__ is declared (MB006)"
    scope:
      - tests/plugins/test_runner.py
      - tests/plugins/test_integration.py
      - tests/plugins/test_loader.py
    description: |
      Principle MB006 (architecture/blocking) is violated in 3 file(s).

      Rule: harness_skills/plugins: __all__ is declared and the boundary is EXPLICIT.
      All imports from outside harness_skills/plugins/ must go through
      harness_skills.plugins (the subpackage root) — never from
      harness_skills.plugins.<module> directly.
      The private function `_record_telemetry` in plugins/runner.py must not be
      imported by any file outside the plugins subpackage.

      Affected files:
        - tests/plugins/test_runner.py:12  — imports _record_telemetry private symbol from harness_skills.plugins.runner
        - tests/plugins/test_integration.py:13  — imports load_plugin_gates from harness_skills.plugins.loader directly
        - tests/plugins/test_integration.py:14  — imports run_plugin_gates from harness_skills.plugins.runner directly
        - tests/plugins/test_loader.py:10  — imports load_plugin_gates from harness_skills.plugins.loader directly

      Refactoring steps:
        1. In tests/plugins/test_runner.py: remove the direct import of `_record_telemetry`;
           refactor tests that call it directly to use `patch("harness_skills.plugins.runner._record_telemetry")`
           without importing the symbol.
        2. In tests/plugins/test_integration.py and test_loader.py: update imports to use
           `from harness_skills.plugins import load_plugin_gates, run_plugin_gates`.
        3. Run `/harness:lint --gate principles` to verify zero remaining violations.
        4. Update or add unit/integration tests covering the refactored code.
    pr_title: "refactor: enforce MB006 architecture across test_runner.py, test_integration.py, test_loader.py"
    pr_body: |
      ## What & Why

      Enforces principle MB006 (architecture/blocking):
      "All imports from outside harness_skills/plugins/ must go through
      harness_skills.plugins (the subpackage root) — never from
      harness_skills.plugins.<module> directly."

      This PR was generated automatically by `/golden-principles-cleanup` after
      detecting 3 violation(s) across the following files:
      - tests/plugins/test_runner.py
      - tests/plugins/test_integration.py
      - tests/plugins/test_loader.py

      ## Changes

      - tests/plugins/test_runner.py: remove direct _record_telemetry import; use patch() without import
      - tests/plugins/test_integration.py: update imports to use harness_skills.plugins public surface
      - tests/plugins/test_loader.py: update imports to use harness_skills.plugins public surface
      - Updated affected call-sites to use the new abstraction

      ## Testing

      - [ ] All refactored code has unit tests
      - [ ] No violations remain: `uv run python -m harness_skills.cli.main evaluate --gate principles --format json`
      - [ ] `/harness:lint` passes with 0 blocking violations
      - [ ] Existing test suite passes: `pytest tests/ -v`

      ## Checklist

      - [ ] PR title follows conventional commits (`refactor:` prefix)
      - [ ] No unrelated changes included
      - [ ] docs/PRINCIPLES.md is up to date (run `/define-principles` if needed)
    generated_at: "2026-03-24T00:00:00Z"
    status: "pending"

  - id: cleanup-mb008-tests-test-scorer-py
    principle_id: "MB008"
    principle_category: "architecture"
    severity: "blocking"
    title: "Enforce architecture rule: harness_dashboard: __all__ is now declared (MB008)"
    scope:
      - tests/test_scorer.py
      - tests/test_data_generator.py
    description: |
      Principle MB008 (architecture/blocking) is violated in 2 file(s).

      Rule: harness_dashboard: __all__ is now declared, listing:
        compute_scores, render_dashboard, generate_dataset,
        ArtifactType, HarnessRecord, PRRecord.
      All imports from outside harness_dashboard/ must go through
      harness_dashboard (the package root) — never from harness_dashboard.<module> directly.

      Affected files:
        - tests/test_scorer.py:18  — from harness_dashboard.models import (...)
        - tests/test_scorer.py:24  — from harness_dashboard.scorer import compute_scores
        - tests/test_scorer.py:102  — from harness_dashboard.scorer import _tier (private symbol)
        - tests/test_data_generator.py:20  — from harness_dashboard.models import ArtifactType, HarnessRecord, PRRecord

      Refactoring steps:
        1. In tests/test_scorer.py: replace deep imports with `from harness_dashboard import compute_scores, HarnessRecord, PRRecord`.
        2. In tests/test_scorer.py:102: remove the import of private `_tier`; refactor the test to
           assert on observable behaviour through the public compute_scores() interface instead.
        3. In tests/test_data_generator.py: replace deep imports with `from harness_dashboard import ArtifactType, HarnessRecord, PRRecord`.
        4. Run `/harness:lint --gate principles` to verify zero remaining violations.
        5. Update or add unit/integration tests covering the refactored code.
    pr_title: "refactor: enforce MB008 architecture across tests/test_scorer.py, tests/test_data_generator.py"
    pr_body: |
      ## What & Why

      Enforces principle MB008 (architecture/blocking):
      "All imports from outside harness_dashboard/ must go through
      harness_dashboard (the package root) — never from harness_dashboard.<module> directly."

      This PR was generated automatically by `/golden-principles-cleanup` after
      detecting 2 violation(s) across the following files:
      - tests/test_scorer.py
      - tests/test_data_generator.py

      ## Changes

      - tests/test_scorer.py: update all deep imports to use harness_dashboard public surface; remove _tier private import
      - tests/test_data_generator.py: update deep imports to use harness_dashboard public surface
      - Updated affected call-sites to use the new abstraction

      ## Testing

      - [ ] All refactored code has unit tests
      - [ ] No violations remain: `uv run python -m harness_skills.cli.main evaluate --gate principles --format json`
      - [ ] `/harness:lint` passes with 0 blocking violations
      - [ ] Existing test suite passes: `pytest tests/ -v`

      ## Checklist

      - [ ] PR title follows conventional commits (`refactor:` prefix)
      - [ ] No unrelated changes included
      - [ ] docs/PRINCIPLES.md is up to date (run `/define-principles` if needed)
    generated_at: "2026-03-24T00:00:00Z"
    status: "pending"

  - id: cleanup-p029-harness-tools-coordinate-py
    principle_id: "P029"
    principle_category: "concurrency"
    severity: "blocking"
    title: "Enforce concurrency rule: Agent SDK sessions must always be driven from (P029)"
    scope:
      - harness_tools/coordinate.py
      - examples/performance_hooks_example.py
      - examples/task_lock_example.py
      - harness_skills/task_lock.py
    description: |
      Principle P029 (concurrency/blocking) is violated in 4 file(s).

      Rule: Agent SDK sessions (`ClaudeSDKClient` / `query()`) must always be driven
      from an async context; the top-level sync-to-async bridge must use
      `anyio.run(main)` — never `asyncio.run()` — so that the anyio backend
      (asyncio or trio) is selected consistently and SDK internals that depend
      on anyio primitives work correctly.

      Affected files:
        - harness_tools/coordinate.py:664  — asyncio.run(main()) at entry point
        - examples/performance_hooks_example.py:237  — asyncio.run(main())
        - examples/task_lock_example.py:229  — asyncio.run(demo_hooks_async())
        - harness_skills/task_lock.py:889  — asyncio.run(main())

      Refactoring steps:
        1. In each affected file, replace `import asyncio` + `asyncio.run(main())`
           with `import anyio` + `anyio.run(main)` at the entry-point block.
        2. Ensure `anyio` is already listed in pyproject.toml dependencies (it is).
        3. Run `/harness:lint --gate principles` to verify zero remaining violations.
        4. Update or add unit/integration tests covering the refactored code.
    pr_title: "refactor: enforce P029 concurrency across coordinate.py, task_lock.py, and examples"
    pr_body: |
      ## What & Why

      Enforces principle P029 (concurrency/blocking):
      "The top-level sync-to-async bridge must use `anyio.run(main)` — never
      `asyncio.run()` — so that the anyio backend is selected consistently."

      This PR was generated automatically by `/golden-principles-cleanup` after
      detecting 4 violation(s) across the following files:
      - harness_tools/coordinate.py
      - examples/performance_hooks_example.py
      - examples/task_lock_example.py
      - harness_skills/task_lock.py

      ## Changes

      - harness_tools/coordinate.py: replace asyncio.run(main()) with anyio.run(main)
      - examples/performance_hooks_example.py: replace asyncio.run(main()) with anyio.run(main)
      - examples/task_lock_example.py: replace asyncio.run(...) with anyio.run(...)
      - harness_skills/task_lock.py: replace asyncio.run(main()) with anyio.run(main)
      - Updated affected call-sites to use the new abstraction

      ## Testing

      - [ ] All refactored code has unit tests
      - [ ] No violations remain: `uv run python -m harness_skills.cli.main evaluate --gate principles --format json`
      - [ ] `/harness:lint` passes with 0 blocking violations
      - [ ] Existing test suite passes: `pytest tests/ -v`

      ## Checklist

      - [ ] PR title follows conventional commits (`refactor:` prefix)
      - [ ] No unrelated changes included
      - [ ] docs/PRINCIPLES.md is up to date (run `/define-principles` if needed)
    generated_at: "2026-03-24T00:00:00Z"
    status: "pending"

# Grafana datasource provisioning — auto-wires Loki on first boot
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    version: 1
    isDefault: true
    orgId: 1
    editable: false
    jsonData:
      maxLines: 5000
      timeout: 60
